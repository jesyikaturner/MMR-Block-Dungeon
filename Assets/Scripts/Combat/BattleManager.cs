using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleManager : MonoBehaviour
{
    // Global timer that all monmons follow?
    // private variables
    //readonly private float _maxBattleTimer = 100f;

    //public float currBattleTimer = 0f;
    //[Range(0.1f, 10f)] public float battleTimerModifider;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // Battle Loop
        //if (currBattleTimer < _maxBattleTimer)
        //    currBattleTimer += battleTimerModifider * Time.deltaTime;
        //else
        //    currBattleTimer = 0f;
    }
}
